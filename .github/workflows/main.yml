name: 📊 Update Profile Data

# Executa automaticamente 1x por dia (meia-noite UTC) ou manualmente sob demanda
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-stats:
    name: Atualizar dados do perfil
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🐍 Gerar animação da cobrinha (fallback diário)
        uses: Platane/snk@v3
        with:
          github_user_name: https-shini
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: 🚀 Publicar no branch output
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: ✅ Confirmar atualização
        run: echo "✅ Dados do perfil atualizados com sucesso em $(date -u)"
