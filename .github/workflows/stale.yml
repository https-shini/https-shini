name: 🗂️ Stale Issues & PRs

on:
  schedule:
    - cron: "0 8 * * 1" # toda segunda-feira às 08h UTC
  workflow_dispatch:

jobs:
  stale:
    name: Marcar e fechar issues/PRs parados
    runs-on: ubuntu-latest

    permissions:
      issues: write
      pull-requests: write

    steps:
      - name: 🗂️ Gerenciar issues e PRs stale
        uses: actions/stale@v9
        with:
          # Issues
          days-before-issue-stale: 30
          days-before-issue-close: 7
          stale-issue-label: "stale"
          stale-issue-message: |
            👋 Esta issue ficou sem atividade por 30 dias.
            Será fechada automaticamente em 7 dias se não houver atualização.
            Se ainda for relevante, deixe um comentário!
          close-issue-message: |
            🔒 Issue fechada automaticamente por inatividade.
            Reabra se necessário.

          # Pull Requests
          days-before-pr-stale: 14
          days-before-pr-close: 7
          stale-pr-label: "stale"
          stale-pr-message: |
            👋 Este PR ficou sem atividade por 14 dias.
            Será fechado automaticamente em 7 dias se não houver atualização.
          close-pr-message: |
            🔒 PR fechado automaticamente por inatividade.
            Reabra quando estiver pronto para continuar.

          # Proteções
          exempt-issue-labels: "pinned,security,bug,enhancement"
          exempt-pr-labels: "pinned,security,work-in-progress,wip"
          exempt-all-assignees: true
          # Issues com alguém atribuído nunca são marcadas como stale
