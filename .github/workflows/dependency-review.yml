name: 🛡️ Dependency Review

on:
  pull_request:
    branches: ["main", "master"]

jobs:
  dependency-review:
    name: Verificar dependências vulneráveis
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write
      security-events: write

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🔍 Revisar dependências do PR
        uses: actions/dependency-review-action@v4
        with:
          # Bloqueia dependências com CVEs de nível moderate, high ou critical
          fail-on-severity: moderate

          # Bloqueia licenças incompatíveis com projetos open source
          deny-licenses: GPL-2.0, AGPL-3.0, GPL-3.0

          # Posta resumo automático no PR
          comment-summary-in-pr: always

          # Verifica vulnerabilidades e licenças
          vulnerability-check: true
          license-check: true

      - name: ✅ Resumo
        if: always()
        run: echo "🛡️ Revisão de dependências concluída em $(date -u)"
